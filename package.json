{
  "name": "gpw-analytics-monorepo",
  "private": true,
  "scripts": {
    "postinstall": "npm ci --prefix frontend",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "start": "npm run start --prefix frontend"
  },
  "engines": {
    "node": "^18.18.0 || >=20"
  }
}
